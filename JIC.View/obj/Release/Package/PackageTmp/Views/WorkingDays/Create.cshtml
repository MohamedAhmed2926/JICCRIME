@using JIC.Crime.View.Models
@model WorkingDaysViewModels
@{
    ViewBag.Title = JIC.Base.Resources.PageTitles.WorkingDays;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>@JIC.Base.Resources.Resources.Vacations </h2>
<div class="row">
    <div class="col-sm-2">
        @Html.Partial("CourtConfiguration", new CourtConfigurationViewModel { CourtConfigurationMode = CourtConfiguration.VacationDaysMode })
    </div>
    <div class="col-sm-5">
        @using (Html.CUpdatePanel(Url.Action("Days", "WorkingDays"), "divWeekdays"))
        {
            //To Do Action
            @Html.Partial("_WeekDays")
        }

    </div>

    <div class="col-sm-5">
        @{Html.RenderAction("CourtCalendar", "CyclesDistribution");}
    </div>
</div>


@section Scripts
                    {
    <script type="text/javascript">
        function Update() {
            debugger;
            $("#divWeekdays").trigger('update');
        }
    </script>
}

