@using JIC.Crime.View.Models
@model CaseSessionViewModel



@{
    List<TabsViewModel> TabsViewModel = new List<TabsViewModel>();
    TabsViewModel.AddRange(new List<TabsViewModel>
{
new TabsViewModel { Name = "إثبات حضور الخصوم", Url = Url.Action("Create","WorkingDays") , IsActive = Model.CaseSessionMode == CaseSession.DefectsAttendance },
    new TabsViewModel { Name = "القرارات و الأحكام", Url = Url.Action("Create","DecisionsController",new { id = Model.id,SessionID = Model.SessionID}) , IsActive = Model.CaseSessionMode == CaseSession.Decisionsandjudgments }
});
}

@Html.Partial("TabsHelper", TabsViewModel)
@Html.Partial("TabsHelper", new List<TabsViewModel>
    {
    new TabsViewModel { Name = "إثبات حضور الخصوم", Url = Url.Action("Index","MinutesOfSession") , IsActive = Model.CaseSessionMode == CaseSession.DefectsAttendance }
    
    })
