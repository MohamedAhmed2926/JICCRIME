@using JIC.Crime.View.Models
@model  TextPredectionsViewModels
@if (ViewData["SessionEnded"] != null && (bool)ViewData["SessionEnded"])
{
    <script>
        debugger;
        window.location.href = "/User/Login?returnUrl=/";
    </script>
}
@*@{

    if (ViewBag.RedirectPage != null)
    {
        <script>
        window.location.replace("@((string)ViewBag.RedirectPage)");
        </script>
    }
    else
    {*@

        @*<h2>@JIC.Base.Resources.Resources.DeleteText</h2>*@

<div id="DeleteDiv">
    @using (Ajax.BeginForm("Delete", new AjaxOptions { HttpMethod = "Post", UpdateTargetId = "DeleteDiv" }))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.TextID)
        @Html.HiddenFor(model => model.TextTitle)
        @Html.HiddenFor(model => model.TextPredectionsDescription)
        @Html.HiddenFor(model => model.CircuitName)
        <div class="row">
            <div class="col-sm-3">
                @Html.LabelFor(m => m.TextTitle)
            </div>
            <div class="col-sm-3">
                @Html.LabelFor(m => m.TextTitle, Model.TextTitle)
            </div>
        </div>
        <div class="row">
            <div class="col-sm-3">
                @Html.LabelFor(m => m.TextPredectionsDescription)
            </div>
            <div class="col-sm-3">
                @Html.LabelFor(m => m.TextPredectionsDescription, Model.TextPredectionsDescription)
            </div>
        </div>
        <div class="row">
            <div class="col-sm-3">
                @Html.LabelFor(m => m.CircuitName)
            </div>
            <div class="col-sm-3">
                @Html.LabelFor(m => m.CircuitName, Model.CircuitName)
            </div>
        </div>
        <div class="row">
            <div class="col-sm-3">
                <input type="submit" value="@JIC.Base.Resources.Resources.Delete" id="Delete" class="btn btn-default" name="action:Delete" />
            </div>
            <div class="col-sm-3">
                <button type="button" class="btn btn-default" data-dismiss="modal">@JIC.Base.Resources.Resources.Cancel</button>
            </div>
        </div>
    }
</div>
    @*}}*@